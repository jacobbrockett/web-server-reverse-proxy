name: Update EC2

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "update-default-conf"
  update-default-conf:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Check workspace file location
        run: |
          cat $GITHUB_WORKSPACE/default.conf

      - name: Save secret to .pem file
        run: | 
          echo $PRIVATE_KEY > private-key.pem
        env:
          PRIVATE_KEY: ${{ secrets.WEBSERVER_CONTAINERS_EC2_KEY }}

      - name: Test connection to remote ec2 server
        run: | 
          ssh -i "private-key.pem" ubuntu@ec2-18-224-18-112.us-east-2.compute.amazonaws.com
          cat /home/ubuntu/test-ssh
          exit
      
